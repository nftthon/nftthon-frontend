import type { NextPage } from 'next';

import { UtilityView } from '../views';

const Utility: NextPage = (props) => {
  return (
    <div>
      <UtilityView />
    </div>
  );
};

export default Utility;
